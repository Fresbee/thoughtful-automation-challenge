# Thoughtful Automation Package Sorter

Create a simple program that sorts packages based upon their dimensions and mass. This repository has the beginnings of an API with unit tests exercising the functionality.

## Run Unit Tests

In order to run the unit tests, run this command in the root of this repository:

```bash
python3 -m unittest
```
